import { connect } from 'react-redux';
import TimeLimit from '../../components/TimeLimit/index'


function mapStateToProps(state) {
    return {
    };
}

function mapDispatchToProps(dispatch) {
    return {

    };
}


export default connect(
    mapStateToProps,
    mapDispatchToProps
)(TimeLimit);